PORT=5000
NODE_ENV=development

# This is a dummy connection string. Replace with actual PostgreSQL credentials.
# Connect to Postgres via the shared transaction-mode pooler (IPv4-only)
# Connect to Postgres via the shared transaction-mode pooler (IPv4-only)
DATABASE_URL=postgresql://postgres.lmgeomswxfkhdpolhxxy:Prakhar123.@aws-1-ap-south-1.pooler.supabase.com:5432/postgres
# Connect to Postgres via the shared session-mode pooler (used for migrations)
DIRECT_URL=postgresql://postgres:Prakhar123.@db.lmgeomswxfkhdpolhxxy.supabase.co:5432/postgres